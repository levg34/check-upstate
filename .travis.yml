language: node_js
node_js:
  - "6.11"
script:
  - echo "Deploying..."
install:
  - npm install -g firebase-tools
  - pwd
  - cd functions
  - npm install
after_success:
  - firebase deploy --token $FIREBASE_TOKEN
branches:
  only:
  - master
